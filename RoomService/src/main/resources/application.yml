server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotel_r?currentSchema=rooms
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:4321}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

